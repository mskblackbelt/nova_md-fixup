# Wrapping and Whitespace

This is a deliberately long paragraph line that runs well past any reasonable wrap width and should be re-flowed by md-fixup once the wrap rule runs, which makes it easy to see at a glance whether the formatter actually touched the document at all.

Short line.
Another short line that should join the one above it.

* A list item whose text also runs on for quite a long way and will need to be wrapped and re-indented correctly by the formatter when it processes this file.
* Short item.
* One more item that is long enough to wrap but not as long as the first one in this list.

There are three trailing spaces after this sentence.   
This line ends with a single trailing space. 
This line has a	tab in the middle of it.


Three blank lines precede this paragraph, which should collapse to one.
